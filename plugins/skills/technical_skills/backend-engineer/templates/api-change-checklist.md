# API Change Checklist

- Is the API backward compatible?
- Does it follow the workspace versioning standard?
- Does it require explicit approval for breaking changes?
- Are request/response contracts documented?
- Are tests included?
