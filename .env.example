# Postgres connection string
DATABASE_URL=postgresql://hermes_agent:hermes_agent@localhost:25434/hermes_agent

# Anthropic API key (used when HERMES_PROVIDER=anthropic)
ANTHROPIC_API_KEY=

# DeepSeek API key (used for DeepSeek models — deepseek-chat / deepseek-reasoner).
# Required only if users select a DeepSeek model in the chat model picker.
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com

# Firecrawl API key — enables the agent's web_search / web_extract tools
# ("web" toolset). https://firecrawl.dev
FIRECRAWL_API_KEY=

PORT=8010

GATEWAY_SERVICE_TOKEN=123456
BFF_SIGNING_KEY=7177ba14e47b867f9f07f2dc75186dd0feba87ae3b2b4a87c81ea3b793231831

GITNEXUS_MCP_URL=http://localhost:8002
GITNEXUS_MCP_TOKEN=123456

RAG_MCP_URL=http://localhost:8000
RAG_MCP_TOKEN=123456

USER_SERVICE_URL=http://localhost:8082
USER_SERVICE_TOKEN=123456

NOTIFICATION_SERVICE_URL=http://localhost:8085
NOTIFICATION_SERVICE_TOKEN=123456

WORKFLOW_BACKEND_URL=http://localhost:8080
WORKFLOW_BACKEND_SERVICE_TOKEN=123456

STORAGE_SERVICE_URL=http://localhost:8090
STORAGE_SERVICE_TOKEN=123456

# Required for vcs_pr_context / vcs_pr_review / vcs_create_pr / vcs_ensure_branch /
# vcs_commit_files — must match vcs-service's internal_access.token / INTERNAL_ACCESS_TOKEN.
# No GITHUB_TOKEN needed: vcs-service resolves a scoped GitHub App installation
# token per-owner internally.
VCS_SERVICE_URL=http://localhost:8088
VCS_SERVICE_TOKEN=123456


